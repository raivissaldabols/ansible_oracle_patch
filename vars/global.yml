# This file contains global Client variables

# patch staging area on local server (used for download patches)
local_stage: ../stage_dir # must not end with / (copy issues)
remote_stage: ~/stage     # must note end with / (copy issues)

# logging
local_log: ../log_dir/

# mos

# setting empty vars to avoid pritn vars failures: overrides in lower
oracle_home: ""
oracle_base: ""
soft_user: ""
soft_group: ""